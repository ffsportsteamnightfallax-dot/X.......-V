```html
<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>💋🙈 X....... V.... - HD Media & Community Feed</title>
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: {
                            50: '#eef2ff',
                            100: '#e0e7ff',
                            500: '#6366f1',
                            600: '#4f46e5',
                            700: '#4338ca',
                        }
                    }
                }
            }
        }
    </script>
    
    <!-- FontAwesome Icons & Inter Font -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0b0f19;
            color: #f8fafc;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }
        ::-webkit-scrollbar-track {
            background: #0f172a;
        }
        ::-webkit-scrollbar-thumb {
            background: #334155;
            border-radius: 3px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #475569;
        }

        /* Heart Pulse Animation */
        @keyframes heartPulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.35); }
            100% { transform: scale(1); }
        }
        .animate-heart {
            animation: heartPulse 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        /* Video Controls Overlay */
        .video-container .video-controls {
            opacity: 1;
            transition: opacity 0.3s ease;
        }

        /* Custom Video Timeline Seeker Range Bar */
        input[type="range"].video-seeker {
            -webkit-appearance: none;
            appearance: none;
            background: transparent;
            cursor: pointer;
            width: 100%;
        }

        input[type="range"].video-seeker:focus {
            outline: none;
        }

        /* Chrome/Safari Scrubber Thumb */
        input[type="range"].video-seeker::-webkit-slider-thumb {
            -webkit-appearance: none;
            height: 14px;
            width: 14px;
            border-radius: 50%;
            background: #6366f1;
            cursor: pointer;
            box-shadow: 0 0 10px rgba(99, 102, 241, 0.9);
            margin-top: -5px;
            transition: transform 0.1s ease;
        }
        input[type="range"].video-seeker::-webkit-slider-thumb:hover {
            transform: scale(1.25);
        }

        /* Firefox Scrubber Thumb */
        input[type="range"].video-seeker::-moz-range-thumb {
            height: 14px;
            width: 14px;
            border-radius: 50%;
            background: #6366f1;
            cursor: pointer;
            border: none;
            box-shadow: 0 0 10px rgba(99, 102, 241, 0.9);
            transition: transform 0.1s ease;
        }

        /* Track Styling */
        input[type="range"].video-seeker::-webkit-slider-runnable-track {
            width: 100%;
            height: 4px;
            cursor: pointer;
            background: rgba(255, 255, 255, 0.25);
            border-radius: 2px;
        }

        input[type="range"].video-seeker::-moz-range-track {
            width: 100%;
            height: 4px;
            cursor: pointer;
            background: rgba(255, 255, 255, 0.25);
            border-radius: 2px;
        }

        .glass-panel {
            background: rgba(15, 23, 42, 0.85);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
        }
        
        .glass-modal {
            background: rgba(5, 9, 20, 0.88);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
        }

        video::-webkit-media-controls {
            display: none !important;
        }

        .video-container:fullscreen {
            width: 100vw;
            height: 100vh;
            background-color: #000;
        }
        .video-container:fullscreen .video-controls {
            opacity: 1;
            padding-bottom: 24px;
        }

        .chat-text a {
            color: #818cf8;
            text-decoration: underline;
            word-break: break-all;
        }

        /* Custom Red-Blue Pulsing Animated Text Header */
        @keyframes redBlueGlow {
            0% {
                text-shadow: 0 0 8px rgba(239, 68, 68, 0.8), 0 0 16px rgba(59, 130, 246, 0.5);
                border-color: rgba(239, 68, 68, 0.6);
            }
            50% {
                text-shadow: 0 0 12px rgba(59, 130, 246, 0.9), 0 0 20px rgba(239, 68, 68, 0.6);
                border-color: rgba(59, 130, 246, 0.6);
            }
            100% {
                text-shadow: 0 0 8px rgba(239, 68, 68, 0.8), 0 0 16px rgba(59, 130, 246, 0.5);
                border-color: rgba(239, 68, 68, 0.6);
            }
        }

        .emo-banner-glow {
            animation: redBlueGlow 2s infinite ease-in-out;
        }
    </style>
</head>
<body class="min-h-screen flex flex-col justify-between selection:bg-brand-500 selection:text-white pb-12">

    <!-- Navigation Bar Header -->
    <header class="sticky top-0 z-40 bg-slate-950/90 border-b border-slate-800/80 backdrop-blur-md">
        <div class="max-w-4xl mx-auto px-3 py-2 space-y-2">
            <!-- Top Header Row -->
            <div class="flex items-center justify-between gap-2">
                <!-- Brand Logo & Name -->
                <div class="flex items-center space-x-2 cursor-pointer" onclick="window.scrollTo({top: 0, behavior: 'smooth'})">
                    <!-- Custom SVG Logo for Kissing Monkey 💋🙈 -->
                    <div class="w-9 h-9 bg-gradient-to-tr from-pink-500 via-purple-600 to-rose-500 rounded-2xl flex items-center justify-center shadow-lg border border-pink-400/30 p-1 shrink-0 relative overflow-hidden group">
                        <svg viewBox="0 0 100 100" class="w-full h-full text-white">
                            <!-- Circular Ring & Stars -->
                            <circle cx="50" cy="50" r="42" fill="none" stroke="#f472b6" stroke-width="4" stroke-dasharray="6,4" />
                            <path d="M 22 25 L 25 20 L 28 25 L 33 28 L 28 31 L 25 36 L 22 31 L 17 28 Z" fill="#fb7185"/>
                            <path d="M 75 22 L 77 18 L 79 22 L 83 24 L 79 26 L 77 30 L 75 26 L 71 24 Z" fill="#fb7185"/>
                            <!-- Monkey Head -->
                            <circle cx="50" cy="52" r="26" fill="#8d5b4c"/>
                            <!-- Ears -->
                            <circle cx="22" cy="52" r="9" fill="#8d5b4c"/>
                            <circle cx="22" cy="52" r="5" fill="#fca5a5"/>
                            <circle cx="78" cy="52" r="9" fill="#8d5b4c"/>
                            <circle cx="78" cy="52" r="5" fill="#fca5a5"/>
                            <!-- Face -->
                            <ellipse cx="50" cy="57" rx="18" ry="14" fill="#fbcfe8"/>
                            <!-- Smiling Mouth -->
                            <path d="M 42 61 Q 50 68 58 61" fill="none" stroke="#8d5b4c" stroke-width="3" stroke-linecap="round"/>
                            <!-- Hands Covering Eyes 🙈 -->
                            <ellipse cx="40" cy="45" rx="8" ry="11" fill="#a16207" transform="rotate(-15 40 45)"/>
                            <ellipse cx="60" cy="45" rx="8" ry="11" fill="#a16207" transform="rotate(15 60 45)"/>
                            <!-- Kiss Lips 💋 -->
                            <path d="M 12 68 C 12 60, 28 58, 30 68 C 28 78, 12 76, 12 68 Z" fill="#ef4444"/>
                            <path d="M 12 68 C 16 63, 26 63, 30 68 C 25 73, 17 73, 12 68 Z" fill="#f43f5e"/>
                        </svg>
                    </div>
                    <div class="flex flex-col">
                        <span class="text-base font-black bg-gradient-to-r from-pink-400 via-rose-300 to-amber-200 bg-clip-text text-transparent tracking-wide leading-none flex items-center gap-1">
                            💋🙈 X....... V....
                        </span>
                        <span class="text-[8px] font-semibold text-pink-400/80 tracking-widest uppercase">Kiss & Shy Feed</span>
                    </div>
                </div>

                <!-- Quick Action Buttons -->
                <div class="flex items-center space-x-2 shrink-0">
                    <!-- Share Web Link Button -->
                    <button id="shareAppLinkBtn" class="bg-gradient-to-r from-pink-600 to-rose-600 hover:from-pink-500 hover:to-rose-500 text-white px-2.5 py-1.5 rounded-xl flex items-center justify-center transition-all shadow-md active:scale-95 text-[10px] font-bold gap-1 border border-pink-400/30" title="Copy link to share">
                        <i class="fa-solid fa-share-nodes text-[11px]"></i>
                        <span>Share App</span>
                    </button>

                    <button id="openUploadModalBtn" class="bg-indigo-600 hover:bg-indigo-500 text-white w-7 h-7 rounded-xl flex items-center justify-center transition-all shadow-md active:scale-95" title="Upload Media">
                        <i class="fa-solid fa-plus text-[10px]"></i>
                    </button>

                    <button id="openChatModalBtn" class="relative w-7 h-7 rounded-xl bg-slate-800/80 border border-slate-700/60 hover:bg-indigo-600 text-indigo-400 hover:text-white flex items-center justify-center transition-all text-[10px] active:scale-95" title="Group Messenger">
                        <i class="fa-solid fa-user"></i>
                        <span id="chatBadge" class="absolute -top-0.5 -right-0.5 w-2 h-2 bg-rose-500 rounded-full border border-slate-900"></span>
                    </button>
                </div>
            </div>

            <!-- Compact Search Input -->
            <div class="relative w-full">
                <i class="fa-solid fa-magnifying-glass absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 text-[10px]"></i>
                <input type="text" id="searchInput" placeholder="Search posts..." 
                    class="w-full bg-slate-900/90 border border-slate-800 rounded-xl pl-8 pr-3 py-1 text-[11px] text-slate-200 placeholder-slate-500 focus:outline-none focus:border-indigo-500 transition-colors">
            </div>

            <!-- Filter Bar & Post Count -->
            <div class="flex items-center justify-between pt-0.5 gap-1.5 overflow-x-auto no-scrollbar">
                <div class="flex items-center space-x-1.5 shrink-0">
                    <button data-filter="all" class="filter-tab px-2.5 py-1 rounded-lg text-[10px] font-semibold bg-indigo-600 text-white shadow-sm transition-all">
                        All Media
                    </button>
                    <button data-filter="image" class="filter-tab px-2.5 py-1 rounded-lg text-[10px] font-medium bg-slate-800/80 text-slate-400 hover:text-slate-200 transition-all border border-slate-700/50">
                        <i class="fa-regular fa-image mr-1 text-[9px]"></i>Photos
                    </button>
                    <button data-filter="video" class="filter-tab px-2.5 py-1 rounded-lg text-[10px] font-medium bg-slate-800/80 text-slate-400 hover:text-slate-200 transition-all border border-slate-700/50">
                        <i class="fa-solid fa-film mr-1 text-[9px]"></i>Videos
                    </button>
                </div>
                <span id="postCountBadge" class="text-[9px] font-semibold text-slate-400 bg-slate-900 px-2 py-0.5 rounded-md border border-slate-800 whitespace-nowrap">
                    0 Posts
                </span>
            </div>
        </div>
    </header>

    <!-- Main Content Container -->
    <main class="max-w-4xl mx-auto px-2 sm:px-4 py-4 w-full flex-grow">
        <div id="feedContainer" class="space-y-6">
            <!-- Dynamic Posts Rendered Here -->
        </div>

        <!-- Empty Feed State -->
        <div id="emptyFeedState" class="hidden flex-col items-center justify-center py-20 px-4 text-center glass-panel rounded-2xl border border-dashed border-slate-800">
            <div class="w-14 h-14 rounded-2xl bg-slate-800/80 border border-slate-700 flex items-center justify-center text-slate-500 mb-3 shadow-inner">
                <i class="fa-solid fa-photo-film text-2xl"></i>
            </div>
            <h3 class="text-sm font-semibold text-slate-200 mb-1">No Posts Found</h3>
            <p class="text-xs text-slate-400 max-w-xs mb-5 leading-relaxed">Be the first to share a photo or video!</p>
            <button onclick="document.getElementById('openUploadModalBtn').click()" class="bg-indigo-600 hover:bg-indigo-500 text-white px-4 py-2 rounded-xl text-xs font-semibold transition-all shadow-md">
                Upload Media
            </button>
        </div>
    </main>

    <!-- Upload Modal -->
    <div id="uploadModal" class="fixed inset-0 z-50 flex items-center justify-center p-3 glass-modal hidden transition-opacity duration-300">
        <div class="bg-slate-900 border border-slate-800 rounded-2xl w-full max-w-md overflow-hidden shadow-2xl relative flex flex-col max-h-[90vh]">
            <div class="px-4 py-3 border-b border-slate-800 flex items-center justify-between bg-slate-950/80">
                <h3 class="text-[11px] font-bold text-slate-100 uppercase tracking-wider flex items-center gap-2">
                    <i class="fa-solid fa-cloud-arrow-up text-indigo-400"></i>
                    Upload Video or Photo
                </h3>
                <button id="closeUploadModalBtn" class="text-slate-400 hover:text-slate-200 p-1 rounded-lg hover:bg-slate-800">
                    <i class="fa-solid fa-xmark text-sm"></i>
                </button>
            </div>

            <form id="uploadForm" class="p-4 space-y-4 overflow-y-auto flex-1 bg-slate-900">
                <div id="dropzone" class="border-2 border-dashed border-slate-700 hover:border-indigo-500 rounded-xl p-6 text-center cursor-pointer transition-all bg-slate-950/50 relative">
                    <input type="file" id="mediaInput" accept="image/jpeg,image/png,image/webp,image/gif,video/mp4,video/webm" multiple class="hidden">
                    <div id="dropzonePrompt" class="space-y-3 py-2">
                        <div class="w-12 h-12 bg-indigo-500/10 text-indigo-400 rounded-xl flex items-center justify-center mx-auto">
                            <i class="fa-solid fa-file-video text-xl"></i>
                        </div>
                        <div>
                            <p class="text-[11px] font-bold text-slate-200">Tap or drag media file here</p>
                            <p class="text-[10px] text-slate-400 mt-1">Supports HD Videos & Photos</p>
                        </div>
                    </div>

                    <div id="previewContainer" class="hidden space-y-2 mt-2">
                        <div class="flex items-center justify-between text-[11px] font-semibold text-slate-300 pb-1 border-b border-slate-800/80">
                            <span id="selectedCountBadge" class="text-indigo-400 font-bold">0 files selected</span>
                            <button type="button" id="clearAllMediaBtn" class="text-rose-400 hover:text-rose-300 text-[10px] font-medium flex items-center gap-1">
                                <i class="fa-solid fa-trash"></i> Clear All
                            </button>
                        </div>
                        <div id="galleryPreviewGrid" class="grid grid-cols-3 gap-2 max-h-48 overflow-y-auto p-1 bg-slate-950/80 rounded-lg border border-slate-800"></div>
                    </div>
                </div>

                <button type="submit" id="submitPostBtn" class="w-full bg-indigo-600 hover:bg-indigo-500 text-white font-semibold py-3 rounded-xl text-xs transition-all shadow-md flex items-center justify-center space-x-2 mt-4">
                    <i class="fa-solid fa-paper-plane text-xs"></i>
                    <span id="submitBtnText">Publish Post</span>
                </button>
            </form>
        </div>
    </div>

    <!-- Community Messenger Group Modal -->
    <div id="chatModal" class="fixed inset-0 z-50 flex items-center justify-center p-2 sm:p-4 glass-modal hidden transition-opacity duration-300">
        <div class="bg-slate-900 border border-slate-800 rounded-2xl w-full max-w-lg h-[88vh] max-h-[680px] shadow-2xl relative flex flex-col overflow-hidden">
            <!-- Header Bar -->
            <div class="px-4 py-3 border-b border-slate-800 flex items-center justify-between bg-slate-950/90 relative z-20">
                <div class="flex items-center space-x-2.5">
                    <div class="w-8 h-8 bg-rose-500/20 text-rose-400 border border-rose-500/30 rounded-full flex items-center justify-center text-sm font-bold">
                        💋🙈
                    </div>
                    <div>
                        <h3 class="text-[11px] font-bold text-slate-100 flex items-center gap-1.5">
                            💋🙈 Community Messenger
                            <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse"></span>
                        </h3>
                        <p class="text-[9px] text-slate-400">Share messages, links, photos & videos</p>
                    </div>
                </div>
                <button id="closeChatModalBtn" class="text-slate-400 hover:text-slate-200 p-1.5 rounded-lg hover:bg-slate-800">
                    <i class="fa-solid fa-xmark text-sm"></i>
                </button>
            </div>

            <!-- 100% Real IMO Service Animated Banner -->
            <div class="bg-slate-950 py-2.5 px-3 border-b border-slate-800/80 flex items-center justify-center shadow-inner relative overflow-hidden">
                <div class="absolute inset-0 bg-gradient-to-r from-red-600/10 via-blue-600/10 to-red-600/10 animate-pulse"></div>
                <div class="relative z-10 flex items-center justify-center gap-2 border border-red-500/50 bg-slate-900/90 px-4 py-1.5 rounded-full emo-banner-glow">
                    <i class="fa-solid fa-shield-halved text-blue-400 text-xs animate-bounce"></i>
                    <span class="text-xs sm:text-sm font-extrabold tracking-wide bg-gradient-to-r from-red-500 via-sky-400 to-blue-500 bg-clip-text text-transparent uppercase">
                        100% REAL IMO SERVICE
                    </span>
                    <i class="fa-solid fa-circle-check text-emerald-400 text-xs"></i>
                </div>
            </div>

            <!-- Messages Area -->
            <div id="chatMessagesContainer" class="flex-1 p-3 space-y-3 overflow-y-auto bg-slate-950/40"></div>

            <!-- Chat Input Form -->
            <form id="chatForm" class="p-2.5 bg-slate-950 border-t border-slate-800 flex flex-col gap-2">
                <div id="chatPhotoPreviewBox" class="hidden relative inline-block max-w-[120px] border border-slate-700 rounded-lg overflow-hidden bg-black">
                    <div id="chatMediaPreviewContent"></div>
                    <button type="button" id="removeChatPhotoBtn" class="absolute top-1 right-1 bg-rose-600 text-white w-4 h-4 rounded-full flex items-center justify-center text-[8px] z-10">
                        <i class="fa-solid fa-xmark"></i>
                    </button>
                </div>
                <div class="flex items-center space-x-2">
                    <label for="chatPhotoInput" class="cursor-pointer p-2 text-slate-400 hover:text-indigo-400 transition-colors" title="Attach Photo or Video">
                        <i class="fa-solid fa-paperclip text-base"></i>
                    </label>
                    <input type="file" id="chatPhotoInput" accept="image/*,video/*" class="hidden">
                    <input type="text" id="chatInput" placeholder="Write a message..." class="flex-1 bg-slate-900 border border-slate-800 rounded-xl px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-indigo-500">
                    <button type="submit" class="bg-indigo-600 hover:bg-indigo-500 text-white px-3.5 py-2 rounded-xl text-xs font-bold transition-all shadow">
                        <i class="fa-solid fa-paper-plane"></i>
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Direct Gallery Download Options Modal -->
    <div id="downloadModal" class="fixed inset-0 z-50 flex items-center justify-center p-4 glass-modal hidden transition-opacity duration-300">
        <div class="bg-slate-900 border border-slate-800 rounded-2xl w-full max-w-xs overflow-hidden shadow-2xl relative flex flex-col p-4 space-y-4">
            <div class="flex items-center justify-between border-b border-slate-800 pb-2">
                <h3 class="text-xs font-bold text-slate-100 flex items-center gap-2">
                    <i class="fa-solid fa-download text-indigo-400"></i>
                    Select Download Quality
                </h3>
                <button id="closeDownloadModalBtn" class="text-slate-400 hover:text-slate-200 text-sm p-1">
                    <i class="fa-solid fa-xmark"></i>
                </button>
            </div>
            <p class="text-[10px] text-slate-400 leading-tight">Choose a format to download (will save directly to your gallery/device):</p>
            <div class="space-y-2">
                <button id="downloadHdBtn" class="w-full bg-slate-800 hover:bg-indigo-600 border border-slate-700/60 hover:border-indigo-500 rounded-xl p-3 flex items-center justify-between group transition-all text-left">
                    <div>
                        <div class="text-xs font-bold text-slate-200 group-hover:text-white flex items-center gap-1.5">
                            <i class="fa-solid fa-circle-check text-indigo-400 group-hover:text-white"></i> High Quality (1080p HD)
                        </div>
                        <div class="text-[9px] text-slate-400 group-hover:text-indigo-100">Saves full HD video</div>
                    </div>
                    <i class="fa-solid fa-file-arrow-down text-sm text-slate-400 group-hover:text-white"></i>
                </button>

                <button id="downloadLowBtn" class="w-full bg-slate-800 hover:bg-emerald-600 border border-slate-700/60 hover:border-emerald-500 rounded-xl p-3 flex items-center justify-between group transition-all text-left">
                    <div>
                        <div class="text-xs font-bold text-slate-200 group-hover:text-white flex items-center gap-1.5">
                            <i class="fa-solid fa-bolt text-emerald-400 group-hover:text-white"></i> Low Quality (360p SD)
                        </div>
                        <div class="text-[9px] text-slate-400 group-hover:text-emerald-100">Low data usage, faster save</div>
                    </div>
                    <i class="fa-solid fa-file-arrow-down text-sm text-slate-400 group-hover:text-white"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- Toast Notification Box -->
    <div id="toast" class="fixed bottom-4 right-4 z-50 bg-slate-900 border border-indigo-500/40 text-slate-100 px-3.5 py-2.5 rounded-xl shadow-2xl flex items-center space-x-2.5 translate-y-20 opacity-0 transition-all duration-300 pointer-events-none text-xs">
        <i id="toastIcon" class="fa-solid fa-circle-check text-emerald-400"></i>
        <span id="toastMessage" class="font-medium">Action successful</span>
    </div>

    <script>
        // IndexedDB Config
        const DB_NAME = 'StreamVaultDB';
        const DB_VERSION = 4; 
        const STORE_NAME = 'media_posts';
        const CHAT_STORE = 'group_chat_messages';
        let db = null;

        function initDB() {
            return new Promise((resolve, reject) => {
                const request = indexedDB.open(DB_NAME, DB_VERSION);

                request.onupgradeneeded = (e) => {
                    const database = e.target.result;
                    if (!database.objectStoreNames.contains(STORE_NAME)) {
                        const store = database.createObjectStore(STORE_NAME, { keyPath: 'id' });
                        store.createIndex('timestamp', 'timestamp', { unique: false });
                    }
                    if (!database.objectStoreNames.contains(CHAT_STORE)) {
                        const chatStore = database.createObjectStore(CHAT_STORE, { keyPath: 'id' });
                        chatStore.createIndex('timestamp', 'timestamp', { unique: false });
                    }
                };

                request.onsuccess = (e) => {
                    db = e.target.result;
                    resolve(db);
                };

                request.onerror = (e) => {
                    console.error('IndexedDB error:', e.target.error);
                    reject('Failed to initialize IndexedDB');
                };
            });
        }

        async function savePostToDB(post) {
            return new Promise((resolve, reject) => {
                const transaction = db.transaction([STORE_NAME], 'readwrite');
                const store = transaction.objectStore(STORE_NAME);
                const request = store.put(post);
                request.onsuccess = () => resolve(true);
                request.onerror = () => reject(request.error);
            });
        }

        async function getAllPostsFromDB() {
            return new Promise((resolve, reject) => {
                const transaction = db.transaction([STORE_NAME], 'readonly');
                const store = transaction.objectStore(STORE_NAME);
                const index = store.index('timestamp');
                const request = index.openCursor(null, 'prev');
                const posts = [];

                request.onsuccess = (e) => {
                    const cursor = e.target.result;
                    if (cursor) {
                        posts.push(cursor.value);
                        cursor.continue();
                    } else {
                        resolve(posts);
                    }
                };
                request.onerror = () => reject(request.error);
            });
        }

        async function saveChatMessage(msg) {
            return new Promise((resolve, reject) => {
                const transaction = db.transaction([CHAT_STORE], 'readwrite');
                const store = transaction.objectStore(CHAT_STORE);
                const request = store.put(msg);
                request.onsuccess = () => resolve(true);
                request.onerror = () => reject(request.error);
            });
        }

        async function getAllChatMessages() {
            return new Promise((resolve, reject) => {
                const transaction = db.transaction([CHAT_STORE], 'readonly');
                const store = transaction.objectStore(CHAT_STORE);
                const index = store.index('timestamp');
                const request = index.openCursor(null, 'next');
                const messages = [];

                request.onsuccess = (e) => {
                    const cursor = e.target.result;
                    if (cursor) {
                        messages.push(cursor.value);
                        cursor.continue();
                    } else {
                        resolve(messages);
                    }
                };
                request.onerror = () => reject(request.error);
            });
        }

        // Global State
        let currentFilter = 'all';
        let searchQuery = '';
        let selectedFiles = []; 
        let activeObjectURLs = [];
        let selectedChatFile = null;
        let selectedPostForDownload = null;

        // DOM Elements
        const uploadModal = document.getElementById('uploadModal');
        const openUploadModalBtn = document.getElementById('openUploadModalBtn');
        const closeUploadModalBtn = document.getElementById('closeUploadModalBtn');
        const dropzone = document.getElementById('dropzone');
        const mediaInput = document.getElementById('mediaInput');
        const dropzonePrompt = document.getElementById('dropzonePrompt');
        const previewContainer = document.getElementById('previewContainer');
        const galleryPreviewGrid = document.getElementById('galleryPreviewGrid');
        const selectedCountBadge = document.getElementById('selectedCountBadge');
        const clearAllMediaBtn = document.getElementById('clearAllMediaBtn');
        const uploadForm = document.getElementById('uploadForm');
        const feedContainer = document.getElementById('feedContainer');
        const emptyFeedState = document.getElementById('emptyFeedState');
        const postCountBadge = document.getElementById('postCountBadge');
        const searchInput = document.getElementById('searchInput');

        // Download Modal Elements
        const downloadModal = document.getElementById('downloadModal');
        const closeDownloadModalBtn = document.getElementById('closeDownloadModalBtn');
        const downloadHdBtn = document.getElementById('downloadHdBtn');
        const downloadLowBtn = document.getElementById('downloadLowBtn');

        // Share App Link Button Logic
        const shareAppLinkBtn = document.getElementById('shareAppLinkBtn');
        if (shareAppLinkBtn) {
            shareAppLinkBtn.addEventListener('click', () => {
                const dummy = document.createElement('input');
                document.body.appendChild(dummy);
                dummy.value = window.location.href;
                dummy.select();
                document.execCommand('copy');
                document.body.removeChild(dummy);
                showToast('Website link copied! Share it with your friends.');
            });
        }

        closeDownloadModalBtn.addEventListener('click', () => downloadModal.classList.add('hidden'));
        downloadModal.addEventListener('click', (e) => {
            if (e.target === downloadModal) downloadModal.classList.add('hidden');
        });

        // Trigger Direct Gallery / Local Download
        downloadHdBtn.addEventListener('click', () => {
            if (selectedPostForDownload) executeDownload(selectedPostForDownload, '1080p_HD');
            downloadModal.classList.add('hidden');
        });

        downloadLowBtn.addEventListener('click', () => {
            if (selectedPostForDownload) executeDownload(selectedPostForDownload, '360p_SD');
            downloadModal.classList.add('hidden');
        });

        function executeDownload(post, qualityTag) {
            if (!post || !post.fileBlob) return;

            const mediaUrl = URL.createObjectURL(post.fileBlob);
            const a = document.createElement('a');
            a.style.display = 'none';
            a.href = mediaUrl;

            let ext = post.type === 'image' 
                ? (post.fileBlob.type ? post.fileBlob.type.split('/')[1] : 'jpg') 
                : (post.fileBlob.type ? post.fileBlob.type.split('/')[1] : 'mp4');

            const safeTitle = (post.title || 'media_file').replace(/[^a-z0-9]/gi, '_').toLowerCase();
            a.download = `${safeTitle}_${qualityTag}.${ext}`;

            document.body.appendChild(a);
            a.click();

            setTimeout(() => {
                document.body.removeChild(a);
                URL.revokeObjectURL(mediaUrl);
            }, 1000);

            showToast(`Download started! File will be saved to your gallery.`);
        }

        // Chat DOM Elements
        const chatModal = document.getElementById('chatModal');
        const openChatModalBtn = document.getElementById('openChatModalBtn');
        const closeChatModalBtn = document.getElementById('closeChatModalBtn');
        const chatMessagesContainer = document.getElementById('chatMessagesContainer');
        const chatForm = document.getElementById('chatForm');
        const chatInput = document.getElementById('chatInput');
        const chatPhotoInput = document.getElementById('chatPhotoInput');
        const chatPhotoPreviewBox = document.getElementById('chatPhotoPreviewBox');
        const chatMediaPreviewContent = document.getElementById('chatMediaPreviewContent');
        const removeChatPhotoBtn = document.getElementById('removeChatPhotoBtn');

        openUploadModalBtn.addEventListener('click', () => uploadModal.classList.remove('hidden'));
        closeUploadModalBtn.addEventListener('click', resetAndCloseModal);
        uploadModal.addEventListener('click', (e) => {
            if (e.target === uploadModal) resetAndCloseModal();
        });

        function resetAndCloseModal() {
            uploadModal.classList.add('hidden');
            uploadForm.reset();
            clearSelectedMedia();
        }

        openChatModalBtn.addEventListener('click', async () => {
            chatModal.classList.remove('hidden');
            await renderChatMessages();
            scrollChatToBottom();
        });

        closeChatModalBtn.addEventListener('click', () => {
            chatModal.classList.add('hidden');
            resetChatInput();
        });

        chatModal.addEventListener('click', (e) => {
            if (e.target === chatModal) {
                chatModal.classList.add('hidden');
                resetChatInput();
            }
        });

        // Chat Media Input Handler (Support Photos & Videos)
        chatPhotoInput.addEventListener('change', (e) => {
            if (e.target.files && e.target.files[0]) {
                const file = e.target.files[0];
                if (!file.type.startsWith('image/') && !file.type.startsWith('video/')) {
                    showToast('Only photo or video uploads are supported.', 'error');
                    return;
                }
                selectedChatFile = file;
                const mediaUrl = URL.createObjectURL(file);

                if (file.type.startsWith('image/')) {
                    chatMediaPreviewContent.innerHTML = `<img class="w-full h-20 object-cover" src="${mediaUrl}" alt="Preview">`;
                } else {
                    chatMediaPreviewContent.innerHTML = `
                        <div class="relative w-full h-20 bg-black flex items-center justify-center">
                            <video src="${mediaUrl}" class="w-full h-full object-cover"></video>
                            <i class="fa-solid fa-play text-white absolute text-xs"></i>
                        </div>
                    `;
                }

                chatPhotoPreviewBox.classList.remove('hidden');
            }
        });

        removeChatPhotoBtn.addEventListener('click', () => {
            selectedChatFile = null;
            chatPhotoInput.value = '';
            chatPhotoPreviewBox.classList.add('hidden');
            chatMediaPreviewContent.innerHTML = '';
        });

        function resetChatInput() {
            chatForm.reset();
            selectedChatFile = null;
            chatPhotoInput.value = '';
            chatPhotoPreviewBox.classList.add('hidden');
            chatMediaPreviewContent.innerHTML = '';
        }

        chatForm.addEventListener('submit', async (e) => {
            e.preventDefault();
            const text = chatInput.value.trim();

            if (!text && !selectedChatFile) return;

            const isVideo = selectedChatFile ? selectedChatFile.type.startsWith('video/') : false;

            const newMsg = {
                id: 'msg_' + Date.now() + '_' + Math.random().toString(36).substr(2, 5),
                sender: 'User',
                text: text,
                mediaBlob: selectedChatFile || null,
                mediaType: isVideo ? 'video' : 'image',
                timestamp: Date.now()
            };

            await saveChatMessage(newMsg);
            resetChatInput();
            await renderChatMessages();
            scrollChatToBottom();
        });

        function scrollChatToBottom() {
            chatMessagesContainer.scrollTop = chatMessagesContainer.scrollHeight;
        }

        async function renderChatMessages() {
            const messages = await getAllChatMessages();

            if (messages.length === 0) {
                chatMessagesContainer.innerHTML = `
                    <div class="h-full flex flex-col items-center justify-center text-center p-6 text-slate-500">
                        <i class="fa-solid fa-comments text-2xl mb-2 text-indigo-400/60"></i>
                        <p class="text-[11px] font-semibold text-slate-300">No messages yet</p>
                        <p class="text-[9px] text-slate-400 mt-1">Send a photo, video or message to get started!</p>
                    </div>
                `;
                return;
            }

            chatMessagesContainer.innerHTML = messages.map(msg => {
                const formattedText = parseLinksAndText(msg.text);
                const mediaUrl = msg.mediaBlob ? URL.createObjectURL(msg.mediaBlob) : (msg.photoBlob ? URL.createObjectURL(msg.photoBlob) : null);
                if (mediaUrl) activeObjectURLs.push(mediaUrl);

                const isVideo = msg.mediaType === 'video';

                const timeStr = new Date(msg.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });

                return `
                    <div class="flex items-start space-x-2">
                        <div class="w-6 h-6 rounded-full bg-indigo-600/40 border border-indigo-500/30 text-indigo-300 font-bold text-[9px] flex items-center justify-center shrink-0 mt-0.5">
                            U
                        </div>
                        <div class="max-w-[82%] bg-slate-800 border border-slate-700 rounded-2xl rounded-tl-sm p-2 shadow-sm">
                            ${msg.text ? `<p class="text-[11px] text-slate-200 leading-relaxed chat-text">${formattedText}</p>` : ''}
                            ${mediaUrl ? (
                                isVideo ? `
                                    <div class="mt-1.5 rounded-lg overflow-hidden border border-slate-700 bg-black">
                                        <video src="${mediaUrl}" controls class="max-h-48 w-full object-contain"></video>
                                    </div>
                                ` : `
                                    <img src="${mediaUrl}" class="mt-1.5 rounded-lg border border-slate-700 max-h-40 object-cover w-full" alt="Shared media">
                                `
                            ) : ''}
                            <span class="text-[8px] text-slate-400 mt-1 block text-right">${timeStr}</span>
                        </div>
                    </div>
                `;
            }).join('');
        }

        function parseLinksAndText(str) {
            if (!str) return '';
            const escaped = escapeHTML(str);
            const urlRegex = /(https?:\/\/[^\s]+)/g;
            return escaped.replace(urlRegex, (url) => {
                return `<a href="${url}" target="_blank" rel="noopener noreferrer">${url}</a>`;
            });
        }

        dropzone.addEventListener('click', (e) => {
            if (e.target.closest('#previewContainer')) return;
            mediaInput.click();
        });

        mediaInput.addEventListener('change', (e) => {
            if (e.target.files && e.target.files.length > 0) {
                handleMultipleFiles(Array.from(e.target.files));
            }
        });

        dropzone.addEventListener('dragover', (e) => {
            e.preventDefault();
            dropzone.classList.add('border-indigo-500', 'bg-indigo-500/5');
        });

        dropzone.addEventListener('dragleave', (e) => {
            e.preventDefault();
            dropzone.classList.remove('border-indigo-500', 'bg-indigo-500/5');
        });

        dropzone.addEventListener('drop', (e) => {
            e.preventDefault();
            dropzone.classList.remove('border-indigo-500', 'bg-indigo-500/5');
            if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
                handleMultipleFiles(Array.from(e.dataTransfer.files));
            }
        });

        function handleMultipleFiles(files) {
            const validFiles = files.filter(f => f.type.startsWith('image/') || f.type.startsWith('video/'));
            if (validFiles.length === 0) {
                showToast('Please upload valid video or image files.', 'error');
                return;
            }
            selectedFiles = [...selectedFiles, ...validFiles];
            renderPreviewGallery();
        }

        function renderPreviewGallery() {
            if (selectedFiles.length === 0) {
                clearSelectedMedia();
                return;
            }

            dropzonePrompt.classList.add('hidden');
            previewContainer.classList.remove('hidden');
            selectedCountBadge.textContent = `${selectedFiles.length} ${selectedFiles.length === 1 ? 'file' : 'files'} selected`;

            galleryPreviewGrid.innerHTML = '';

            selectedFiles.forEach((file, index) => {
                const isVideo = file.type.startsWith('video/');
                const objectUrl = URL.createObjectURL(file);

                const itemWrapper = document.createElement('div');
                itemWrapper.className = 'relative aspect-square rounded-lg overflow-hidden bg-black border border-slate-800 group shadow-md';

                if (isVideo) {
                    itemWrapper.innerHTML = `
                        <video src="${objectUrl}" class="w-full h-full object-cover"></video>
                        <div class="absolute inset-0 flex items-center justify-center bg-black/30 pointer-events-none">
                            <i class="fa-solid fa-play text-white text-[10px] drop-shadow"></i>
                        </div>
                        <div class="absolute top-1 left-1 bg-black/80 px-1 py-0.5 rounded text-[8px] font-bold text-indigo-400">
                            VIDEO
                        </div>
                    `;
                } else {
                    itemWrapper.innerHTML = `
                        <img src="${objectUrl}" class="w-full h-full object-cover" alt="Preview">
                        <div class="absolute top-1 left-1 bg-black/80 px-1 py-0.5 rounded text-[8px] font-bold text-emerald-400">
                            PHOTO
                        </div>
                    `;
                }

                const removeBtn = document.createElement('button');
                removeBtn.type = 'button';
                removeBtn.className = 'absolute top-1 right-1 bg-rose-600/90 text-white w-4 h-4 rounded-full flex items-center justify-center shadow hover:bg-rose-500 transition-colors z-10';
                removeBtn.innerHTML = '<i class="fa-solid fa-xmark text-[8px]"></i>';
                removeBtn.onclick = (e) => {
                    e.stopPropagation();
                    selectedFiles.splice(index, 1);
                    renderPreviewGallery();
                };

                itemWrapper.appendChild(removeBtn);
                galleryPreviewGrid.appendChild(itemWrapper);
            });
        }

        clearAllMediaBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            clearSelectedMedia();
        });

        function clearSelectedMedia() {
            selectedFiles = [];
            mediaInput.value = '';
            galleryPreviewGrid.innerHTML = '';
            previewContainer.classList.add('hidden');
            dropzonePrompt.classList.remove('hidden');
        }

        uploadForm.addEventListener('submit', async (e) => {
            e.preventDefault();

            if (selectedFiles.length === 0) {
                showToast('Please select photos or videos to upload.', 'error');
                return;
            }

            const submitBtn = document.getElementById('submitPostBtn');
            submitBtn.disabled = true;
            submitBtn.classList.add('opacity-75');

            let uploadedCount = 0;

            for (let i = 0; i < selectedFiles.length; i++) {
                const file = selectedFiles[i];
                const mediaType = file.type.startsWith('image/') ? 'image' : 'video';

                let durationStr = '0:00';
                if (mediaType === 'video') {
                    durationStr = await getVideoDuration(file);
                }

                const generatedTitle = `Upload ${Math.floor(Math.random() * 900) + 100}`;
                
                const newPost = {
                    id: 'post_' + Date.now() + '_' + Math.random().toString(36).substr(2, 7),
                    title: generatedTitle,
                    channel: 'Local Creator',
                    type: mediaType,
                    fileBlob: file,
                    duration: durationStr,
                    views: Math.floor(Math.random() * 90) + 10 + 'K views',
                    likes: Math.floor(Math.random() * 50) + 5,
                    isLiked: false,
                    currentQuality: '1080p',
                    timestamp: Date.now() - (i * 1000)
                };

                await savePostToDB(newPost);
                uploadedCount++;
            }

            submitBtn.disabled = false;
            submitBtn.classList.remove('opacity-75');

            showToast(`${uploadedCount} ${uploadedCount === 1 ? 'post' : 'posts'} published successfully!`);
            resetAndCloseModal();
            await renderFeed();
        });

        function getVideoDuration(file) {
            return new Promise((resolve) => {
                const video = document.createElement('video');
                video.preload = 'metadata';
                video.src = URL.createObjectURL(file);
                video.onloadedmetadata = () => {
                    URL.revokeObjectURL(video.src);
                    const minutes = Math.floor(video.duration / 60);
                    const seconds = Math.floor(video.duration % 60);
                    resolve(`${minutes}:${seconds < 10 ? '0' : ''}${seconds}`);
                };
                video.onerror = () => resolve('0:10');
            });
        }

        async function renderFeed() {
            activeObjectURLs.forEach(url => URL.revokeObjectURL(url));
            activeObjectURLs = [];

            let posts = await getAllPostsFromDB();

            if (searchQuery) {
                const q = searchQuery.toLowerCase();
                posts = posts.filter(p => p.title.toLowerCase().includes(q) || p.channel.toLowerCase().includes(q));
            }

            if (currentFilter !== 'all') {
                posts = posts.filter(p => p.type === currentFilter);
            }

            postCountBadge.textContent = `${posts.length} ${posts.length === 1 ? 'Post' : 'Posts'}`;

            if (posts.length === 0) {
                feedContainer.innerHTML = '';
                emptyFeedState.classList.remove('hidden');
                emptyFeedState.classList.add('flex');
                return;
            }

            emptyFeedState.classList.add('hidden');
            emptyFeedState.classList.remove('flex');

            feedContainer.innerHTML = posts.map(post => createPostHTML(post)).join('');
            attachPostEvents(posts);
        }

        // Post Card Template
        function createPostHTML(post) {
            const mediaObjectURL = URL.createObjectURL(post.fileBlob);
            activeObjectURLs.push(mediaObjectURL);
            const timeAgo = formatTimeAgo(post.timestamp);

            return `
                <article class="bg-black/90 rounded-2xl overflow-hidden border border-slate-800/80 shadow-2xl transition-all duration-300" data-id="${post.id}">
                    <div class="relative w-full aspect-video bg-black flex items-center justify-center overflow-hidden group video-container">
                        ${post.type === 'image' 
                            ? `<img src="${mediaObjectURL}" class="w-full h-full object-cover select-none" alt="Photo Upload">` 
                            : `
                                <video id="video_${post.id}" src="${mediaObjectURL}" class="w-full h-full object-contain cursor-pointer" playsinline></video>
                                
                                <div class="video-controls absolute inset-0 bg-gradient-to-t from-black/90 via-transparent to-black/30 flex flex-col justify-between p-3 pointer-events-none">
                                    <div class="flex justify-between items-center pointer-events-auto">
                                        <div class="bg-pink-600/80 backdrop-blur border border-pink-500/50 px-2 py-0.5 rounded-full text-[9px] font-bold text-white">💋🙈 Feed</div>
                                        <div class="bg-black/70 border border-slate-700/80 px-2 py-0.5 rounded text-[10px] font-bold text-white tracking-widest shadow-sm">HD</div>
                                    </div>

                                    <!-- Bottom Video Scrubber & Control Bar -->
                                    <div class="space-y-1.5 pointer-events-auto">
                                        <!-- Custom Seeker Slider Bar -->
                                        <div class="relative flex items-center w-full">
                                            <input type="range" class="video-seeker progress-bar w-full h-1.5 rounded-lg cursor-pointer" value="0" min="0" max="100" step="0.1">
                                        </div>

                                        <div class="flex items-center justify-between text-white text-xs pt-0.5">
                                            <div class="flex items-center space-x-2.5">
                                                <button class="play-pause-btn text-white hover:text-indigo-400 transition-colors p-1">
                                                    <i class="fa-solid fa-play text-xs"></i>
                                                </button>
                                                <button class="mute-btn text-slate-300 hover:text-white transition-colors p-1">
                                                    <i class="fa-solid fa-volume-high text-[10px]"></i>
                                                </button>
                                                <span class="time-display text-[10px] font-mono text-slate-300">0:00 / ${post.duration || '0:10'}</span>
                                            </div>

                                            <div class="flex items-center space-x-2 relative">
                                                <button class="quality-btn bg-indigo-600/90 hover:bg-indigo-600 px-2 py-0.5 rounded text-[9px] font-bold text-white flex items-center gap-1 shadow">
                                                    <span class="quality-label">${post.currentQuality || '1080p'}</span>
                                                    <i class="fa-solid fa-gear text-[8px]"></i>
                                                </button>
                                                
                                                <div class="quality-menu hidden absolute bottom-8 right-0 bg-slate-900 border border-slate-700 rounded-xl p-1.5 shadow-2xl w-28 space-y-1 z-30 text-xs">
                                                    <div class="text-[8px] uppercase font-bold text-slate-400 px-2 py-0.5 border-b border-slate-800">Quality</div>
                                                    <button class="quality-opt w-full text-left px-2 py-1 rounded hover:bg-slate-800 text-[10px] text-emerald-400 font-semibold flex items-center justify-between" data-quality="1080p">
                                                        <span>1080p HD</span> <i class="fa-solid fa-check text-[8px]"></i>
                                                    </button>
                                                    <button class="quality-opt w-full text-left px-2 py-1 rounded hover:bg-slate-800 text-[10px] text-slate-300" data-quality="720p">720p</button>
                                                    <button class="quality-opt w-full text-left px-2 py-1 rounded hover:bg-slate-800 text-[10px] text-slate-300" data-quality="360p">360p</button>
                                                </div>

                                                <button class="fullscreen-btn text-slate-300 hover:text-white transition-colors p-1">
                                                    <i class="fa-solid fa-expand text-[10px]"></i>
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            `
                        }
                        <div class="absolute bottom-2 right-2 bg-black/80 px-1.5 py-0.5 rounded text-[9px] font-mono font-bold text-white tracking-wide border border-slate-800 pointer-events-none">
                            ${post.type === 'video' ? (post.duration || '0:10') : 'PHOTO'}
                        </div>
                    </div>

                    <div class="p-3 flex items-center justify-between gap-3 bg-slate-950">
                        <div class="w-8 h-8 rounded-full bg-gradient-to-tr from-pink-500 to-rose-600 flex items-center justify-center font-bold text-xs text-white shrink-0 border border-pink-400/30 shadow">
                            💋
                        </div>
                        <div class="flex-1 min-w-0">
                            <h3 class="font-bold text-slate-100 text-[11px] sm:text-xs truncate leading-tight tracking-tight">${post.title}</h3>
                            <p class="text-[9px] text-slate-400 mt-0.5 flex items-center gap-1">
                                <span>${escapeHTML(post.channel)}</span> • <span>${post.views || '79K views'}</span> • <span>${timeAgo}</span>
                            </p>
                        </div>
                        <div class="flex items-center space-x-2 shrink-0">
                            <!-- Kiss & Shy Reaction Quick Buttons -->
                            <button class="kiss-react-btn text-slate-400 hover:text-pink-400 transition-all active:scale-125 p-1 text-sm" data-id="${post.id}" title="Send Kiss 💋">
                                💋
                            </button>
                            <button class="shy-react-btn text-slate-400 hover:text-amber-300 transition-all active:scale-125 p-1 text-sm" data-id="${post.id}" title="Send Shy Monkey 🙈">
                                🙈
                            </button>
                            <button class="like-btn flex items-center space-x-1 text-slate-400 hover:text-rose-500 transition-colors ml-1" data-id="${post.id}">
                                <i class="${post.isLiked ? 'fa-solid text-rose-500 animate-heart' : 'fa-regular'} fa-heart text-[13px]"></i>
                                <span class="like-count text-[10px] font-bold text-slate-300">${post.likes}</span>
                            </button>
                            <!-- Download Button -->
                            <button class="download-post-btn text-slate-400 hover:text-indigo-400 transition-colors p-1" title="Download Video/Photo">
                                <i class="fa-solid fa-download text-xs"></i>
                            </button>
                        </div>
                    </div>
                </article>
            `;
        }

        // Attach event listeners for single video playback (auto-pause others) & custom seeker
        function attachPostEvents(posts) {
            document.querySelectorAll('article[data-id]').forEach(card => {
                const postId = card.getAttribute('data-id');
                const post = posts.find(p => p.id === postId);
                const video = card.querySelector('video');
                const videoContainer = card.querySelector('.video-container');

                if (video) {
                    const playPauseBtn = card.querySelector('.play-pause-btn');
                    const playIcon = playPauseBtn ? playPauseBtn.querySelector('i') : null;
                    const muteBtn = card.querySelector('.mute-btn');
                    const muteIcon = muteBtn ? muteBtn.querySelector('i') : null;
                    const progressBar = card.querySelector('.progress-bar');
                    const timeDisplay = card.querySelector('.time-display');
                    const fullscreenBtn = card.querySelector('.fullscreen-btn');
                    const qualityBtn = card.querySelector('.quality-btn');
                    const qualityMenu = card.querySelector('.quality-menu');
                    const qualityLabel = card.querySelector('.quality-label');

                    // Auto Pause Logic: Stop all other playing videos
                    const stopOtherVideos = () => {
                        document.querySelectorAll('video').forEach(v => {
                            if (v !== video && !v.paused) {
                                v.pause();
                                const otherCard = v.closest('article');
                                if (otherCard) {
                                    const icon = otherCard.querySelector('.play-pause-btn i');
                                    if (icon) icon.className = 'fa-solid fa-play text-xs';
                                }
                            }
                        });
                    };

                    video.addEventListener('play', () => {
                        stopOtherVideos();
                        if (playIcon) playIcon.className = 'fa-solid fa-pause text-xs';
                    });

                    video.addEventListener('pause', () => {
                        if (playIcon) playIcon.className = 'fa-solid fa-play text-xs';
                    });

                    const togglePlay = () => {
                        if (video.paused) {
                            stopOtherVideos();
                            video.play();
                        } else {
                            video.pause();
                        }
                    };

                    if (playPauseBtn) playPauseBtn.addEventListener('click', togglePlay);
                    video.addEventListener('click', togglePlay);

                    if (muteBtn) {
                        muteBtn.addEventListener('click', () => {
                            video.muted = !video.muted;
                            if (muteIcon) {
                                muteIcon.className = video.muted 
                                    ? 'fa-solid fa-volume-xmark text-[10px] text-rose-400' 
                                    : 'fa-solid fa-volume-high text-[10px] text-slate-300';
                            }
                        });
                    }

                    // Seeker progress update during video playing
                    video.addEventListener('timeupdate', () => {
                        if (video.duration) {
                            const percent = (video.currentTime / video.duration) * 100;
                            if (progressBar) progressBar.value = percent;
                            
                            const curM = Math.floor(video.currentTime / 60);
                            const curS = Math.floor(video.currentTime % 60);
                            const durM = Math.floor(video.duration / 60);
                            const durS = Math.floor(video.duration % 60);
                            if (timeDisplay) {
                                timeDisplay.textContent = `${curM}:${curS < 10 ? '0' : ''}${curS} / ${durM}:${durS < 10 ? '0' : ''}${durS}`;
                            }
                        }
                    });

                    // Drag or click seeker timeline
                    if (progressBar) {
                        progressBar.addEventListener('input', () => {
                            if (video.duration) {
                                video.currentTime = (progressBar.value / 100) * video.duration;
                            }
                        });
                    }

                    if (fullscreenBtn && videoContainer) {
                        fullscreenBtn.addEventListener('click', () => {
                            if (!document.fullscreenElement) {
                                if (videoContainer.requestFullscreen) videoContainer.requestFullscreen();
                                else if (videoContainer.webkitRequestFullscreen) videoContainer.webkitRequestFullscreen();
                            } else {
                                if (document.exitFullscreen) document.exitFullscreen();
                                else if (document.webkitExitFullscreen) document.webkitExitFullscreen();
                            }
                        });
                    }

                    if (qualityBtn && qualityMenu) {
                        qualityBtn.addEventListener('click', (e) => {
                            e.stopPropagation();
                            qualityMenu.classList.toggle('hidden');
                        });

                        card.querySelectorAll('.quality-opt').forEach(opt => {
                            opt.addEventListener('click', (e) => {
                                e.stopPropagation();
                                const q = opt.getAttribute('data-quality');
                                qualityLabel.textContent = q;
                                qualityMenu.classList.add('hidden');
                                showToast(`Video quality set to ${q}`);
                            });
                        });
                    }
                }

                const likeBtn = card.querySelector('.like-btn');
                if (likeBtn) likeBtn.addEventListener('click', () => toggleLike(postId));

                const downloadBtn = card.querySelector('.download-post-btn');
                if (downloadBtn && post) {
                    downloadBtn.addEventListener('click', () => openDownloadOptions(post));
                }
            });
        }

        function openDownloadOptions(post) {
            selectedPostForDownload = post;
            downloadModal.classList.remove('hidden');
        }

        async function toggleLike(postId) {
            const transaction = db.transaction([STORE_NAME], 'readwrite');
            const store = transaction.objectStore(STORE_NAME);
            const request = store.get(postId);

            request.onsuccess = async () => {
                const post = request.result;
                if (!post) return;

                post.isLiked = !post.isLiked;
                post.likes += post.isLiked ? 1 : -1;

                await savePostToDB(post);
                await renderFeed();
            };
        }

        document.querySelectorAll('.filter-tab').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.filter-tab').forEach(b => {
                    b.classList.remove('bg-indigo-600', 'text-white', 'border-transparent');
                    b.classList.add('bg-slate-800/80', 'text-slate-400', 'border-slate-700/50');
                });
                btn.classList.remove('bg-slate-800/80', 'text-slate-400', 'border-slate-700/50');
                btn.classList.add('bg-indigo-600', 'text-white', 'border-transparent');

                currentFilter = btn.getAttribute('data-filter');
                renderFeed();
            });
        });

        searchInput.addEventListener('input', (e) => {
            searchQuery = e.target.value.trim();
            renderFeed();
        });

        function showToast(message, type = 'success') {
            const toast = document.getElementById('toast');
            const toastMessage = document.getElementById('toastMessage');
            const toastIcon = document.getElementById('toastIcon');

            toastMessage.textContent = message;
            toastIcon.className = type === 'error' 
                ? 'fa-solid fa-circle-exclamation text-rose-400' 
                : 'fa-solid fa-circle-check text-emerald-400';

            toast.classList.remove('translate-y-20', 'opacity-0', 'pointer-events-none');
            setTimeout(() => {
                toast.classList.add('translate-y-20', 'opacity-0', 'pointer-events-none');
            }, 2500);
        }

        function formatTimeAgo(timestamp) {
            const seconds = Math.floor((Date.now() - timestamp) / 1000);
            if (seconds < 60) return 'Just now';
            const minutes = Math.floor(seconds / 60);
            if (minutes < 60) return `${minutes}m ago`;
            const hours = Math.floor(minutes / 60);
            if (hours < 24) return `${hours}h ago`;
            const days = Math.floor(hours / 24);
            return `${days}d ago`;
        }

        function escapeHTML(str) {
            if (!str) return '';
            return str.replace(/[&<>'"]/g, 
                tag => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[tag] || tag)
            );
        }

        window.onload = async function() {
            try {
                await initDB();
                await renderFeed();
            } catch (err) {
                console.error("Initialization error:", err);
                showToast("Failed to initialize database", "error");
            }
        };
    </script>
</body>
</html>
```
